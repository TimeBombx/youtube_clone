-# - if errors
  %ul
    - errors.each do |key, value|
      - next if key == :error
      %li= "#{key.to_s.capitalize} #{value}"

- if errors
  - errors.each do |key, value|
    - next if key == :error
    
    .callout.alert{"data-closable" => ""}
      = "#{key.to_s.capitalize} #{value}"
      %button.close-button{"aria-label" => "Dismiss alert", "data-close" => "", :type => "button"}
        %span{"aria-hidden" => "true"} ×